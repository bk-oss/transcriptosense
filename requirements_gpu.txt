# ============================================================
# TranscriptoSense — GPU (Colab / Kaggle) Requirements
# Run this at the top of your Colab notebook:
#   !pip install -r requirements_gpu.txt
#
# Colab already has: torch, numpy, pandas, transformers
# This file adds/upgrades what's missing.
# ============================================================

# ── Core (upgrade to latest if already present) ──────────────
--upgrade torch torchvision torchaudio
transformers>=4.40.0
accelerate>=0.30.0          # Faster model loading on GPU

# ── ASR ─────────────────────────────────────────────────────
faster-whisper>=1.0.0       # Use large-v3 on GPU (float16)
openai-whisper>=20231117

# ── Diarization ─────────────────────────────────────────────
pyannote.audio>=3.1.0
# Set HF token in Colab: from google.colab import userdata

# ── NLP ─────────────────────────────────────────────────────
sentence-transformers>=2.7.0
keybert>=0.8.3
spacy>=3.7.0
camel-tools>=1.5.2
langdetect>=1.0.9
ftfy>=6.1.3

# ── Audio ────────────────────────────────────────────────────
soundfile>=0.12.1
librosa>=0.10.1
pydub>=0.25.1
webrtcvad>=2.0.10

# ── Eval ────────────────────────────────────────────────────
jiwer>=3.0.3
rouge-score>=0.1.2
bert-score>=0.3.13

# ── Summarization ────────────────────────────────────────────
sumy>=0.11.0

# ── Export ──────────────────────────────────────────────────
python-docx>=1.1.0
reportlab>=4.1.0

# ── Misc ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0.1
tqdm>=4.66.0
pandas>=2.2.0
scikit-learn>=1.4.0
loguru>=0.7.2
rich>=13.7.0
